# Individual contribution report

## Thông tin

- Họ và tên: Doãn Hữu Nguyên
- Mã học viên: 2A202602671
- Nhóm: intel
- Repository/branch: K4-L3B-intel-RAG-Pipeline @ devnguyen

## Phần việc đã thực hiện

| Module/deliverable | Việc tôi trực tiếp làm | File/commit/PR | Trạng thái |
|---|---|---|---|
| Tích hợp code nhóm | Merge `origin/main` (bản implement Task 1-10 + `src/evaluate.py` của teammate, chủ đề Du lịch Hà Nội) vào branch `devnguyen` | toàn bộ `src/`, `app.py`, `data/` sau merge | Done (local, chưa push) |
| Cấu hình provider | Thiết lập `.env` dùng OpenAI cho generation (`gpt-4o`) và embedding (`text-embedding-3-small`) | `.env` | Done |
| Fix bug evaluation | Sửa `src/evaluate.py`: đổi `OpenAI` (sync) sang `AsyncOpenAI` khi khởi tạo judge/embeddings cho Ragas — bản gốc bị `TypeError: Cannot use agenerate() with a synchronous client` | `src/evaluate.py` (`build_metrics()`) | Done (chưa commit) |
| Verify pipeline | Chạy end-to-end: `python -m src.task4_chunking_indexing`, `pytest -q`, `streamlit run app.py` | output pytest, `chroma_db/` | Done — 20/20 test pass |
| Chạy evaluation thật | Chạy `python -m src.evaluate` (15 câu golden × 2 config, OpenAI thật), phân tích 3 câu tệ nhất | `group_project/evaluation/RESULT.md`, `raw_results.json` | Done |

## Quyết định kỹ thuật quan trọng

1. **Quyết định:** Dùng OpenAI (`gpt-4o` + `text-embedding-3-small`) thay vì Gemini (mặc định ban đầu trong code teammate) cho cả generation và evaluator.
   **Lý do/evidence:** Đã có sẵn `OPENAI_API_KEY` hoạt động; sửa `src/evaluate.py` để dispatch theo `LLM_PROVIDER`/`EMBEDDING_PROVIDER` thay vì hard-code Gemini.
   **Trade-off:** `gpt-4o` chậm/đắt hơn bản mini; chạy evaluation 30 case (15 câu × 2 config) mất ~11 phút.

2. **Quyết định:** Sửa client Ragas judge/embeddings từ `OpenAI` (sync) sang `AsyncOpenAI`.
   **Lý do/evidence:** Ragas 0.4 `metric.ascore()` gọi `agenerate()` nội bộ, cần client bất đồng bộ; client sync gây crash toàn bộ evaluation ở lần chạy đầu.
   **Trade-off:** Không đáng kể — chỉ đổi đúng dòng khởi tạo client, không ảnh hưởng logic chấm điểm.

## Kiểm thử và kết quả

- Test đã dùng: `pytest tests/test_contracts.py -q` (pass, không gọi network), `pytest -q` toàn bộ (19 passed/1 failed trước khi có `RESULT.md`, sau khi chạy `evaluate.py` xong: **20/20 pass**).
- Kết quả trước/sau: trước khi fix bug, `python -m src.evaluate` crash ngay ở case đầu (`TypeError`). Sau fix: chạy hoàn tất 30 case, sinh `RESULT.md` với số liệu thật — hybrid+RRF thắng nhẹ (trung bình 0.7234 vs dense-only 0.7213, chênh +0.0021), context recall là điểm yếu nhất của cả hai config.
- Lỗi đã phát hiện và cách xử lý: `TypeError: Cannot use agenerate() with a synchronous client` khi khởi tạo Ragas judge — xử lý bằng cách đổi sang `AsyncOpenAI` (xem mục Quyết định kỹ thuật #2). Ngoài ra qua phân tích 3 câu tệ nhất, phát hiện retrieval hay chỉ lấy `chunk-0`/`chunk-1` của một bài dài, bỏ sót `chunk-2` chứa câu trả lời đúng (ví dụ câu về sự kiện lịch sử tại Nhà hát lớn) — dẫn đến answer relevance và context recall = 0 dù faithfulness vẫn = 1 (model từ chối đúng thay vì bịa).

## Điều còn hạn chế

- Retrieval hiện chưa lấy các chunk liền kề khi câu trả lời trải dài qua nhiều chunk của cùng một bài — làm giảm context recall/precision ở các câu liệt kê nhiều ý (ví dụ danh sách công trình kiến trúc Pháp).
- Nếu có thêm thời gian, thay đổi đầu tiên: thêm neighbor-chunk expansion (khi một chunk khớp mạnh, lấy thêm chunk liền trước/sau cùng tài liệu) hoặc tăng `CHUNK_SIZE`/`CHUNK_OVERLAP` cho `data/standardized/news/`, rồi so A/B lại trên đúng 15 câu golden.
- Fix bug và merge hiện chưa được commit/push (git chưa cấu hình `user.name`/`user.email` trên máy).

## Xác nhận đóng góp

Tôi xác nhận nội dung trên phản ánh đúng phần việc của mình và có thể giải thích hoặc chạy lại trong buổi demo.

- Ngày: 2026-09-25
- Tên thành viên: Doãn Hữu Nguyên
