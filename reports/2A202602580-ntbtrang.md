# Individual contribution report

## Thông tin

- Họ và tên: Nguyễn Thị Bảo Trang
- Mã học viên: 2A202602580
- Nhóm: intel
- Repository/branch: https://github.com/J4ck13Ch4n/K4-L3B-intel-RAG-Pipeline.git

## Phần việc đã thực hiện

| Module/deliverable | Việc tôi trực tiếp làm | File/commit/PR | Trạng thái |
|---|---|---|---|
| Thu thập và chuẩn hóa dữ liệu | Chọn đề tài Du lịch Hà Nội; thu thập 3 PDF chính thức và crawl 5 bài của Sở Du lịch Hà Nội; chuyển thành Markdown có metadata nguồn | `data/landing/`, `data/standardized/`, `src/task1_*`–`src/task3_*`; commit `589fcc7` | Done |
| Index và hybrid retrieval | Triển khai chunk ID ổn định, embedding theo provider, ChromaDB upsert, dense search, BM25, RRF và fallback PageIndex | `src/task4_chunking_indexing.py`–`src/task9_retrieval_pipeline.py`; commit `589fcc7` | Done về code; Partial về chạy thực tế |
| Generation và chatbot | Triển khai Gemini/OpenAI/Anthropic dispatch, context có nhãn `[S#]`, safe refusal và giao diện Streamlit hiển thị nguồn, method, score | `src/task10_generation.py`, `app.py`; commit `589fcc7` | Partial — chưa chạy end-to-end sau indexing |
| Golden dataset và evaluation | Soạn 15 câu hỏi grounded; viết runner so sánh dense-only với hybrid + RRF theo 4 metric | `group_project/evaluation/golden_dataset.json`, `src/evaluate.py`; commit `589fcc7` | Partial — chưa sinh kết quả Ragas |
| Tài liệu và cấu hình | Cập nhật README theo đề tài, cấu hình Gemini mẫu, batch embedding và hướng dẫn chạy/test | `README.md`, `.env.example`, `pyproject.toml`; commit `589fcc7` | Done |

## Quyết định kỹ thuật quan trọng

1. **Quyết định:** Xây corpus hai lớp gồm văn bản chính thức và bài cẩm nang của cơ quan du lịch Hà Nội.
   - **Lý do/evidence:** Ba PDF cung cấp dữ liệu quy hoạch/chính sách; năm bài viết bổ sung điểm đến, kiến trúc, ẩm thực và trải nghiệm theo mùa. Mọi tài liệu đều giữ URL và metadata để kiểm tra citation.
   - **Trade-off:** Nguồn có độ tin cậy cao nhưng phạm vi thông tin vận hành như giá vé và giờ mở cửa còn hạn chế.

2. **Quyết định:** Dùng dense + BM25 rồi fuse RRF đúng một lần; quyết định fallback bằng cosine score gốc của dense.
   - **Lý do/evidence:** Tuân thủ module contract, tránh cộng trực tiếp hai thang điểm khác nhau và giữ được lợi thế của cả tìm kiếm ngữ nghĩa lẫn từ khóa/địa danh.
   - **Trade-off:** Pipeline có nhiều dependency và độ trễ cao hơn dense-only; threshold cần được hiệu chỉnh sau khi index corpus.

## Kiểm thử và kết quả

- **Test hoặc query tôi đã dùng:** `pytest tests/test_contracts.py -q`, `pytest -q`, kiểm tra cú pháp bằng `python -m compileall`, kiểm tra JSON; xây các query như “Cổ Loa cách trung tâm Hà Nội bao xa?” và “Phở Bát Đàn ở đâu?”.
- **Kết quả:** Contract tests đạt `15/15`; toàn bộ suite đạt `19/20`. Ba PDF, năm JSON, tám Markdown và 15 golden cases đều đạt yêu cầu acceptance về số lượng/định dạng.
- **Lỗi đã phát hiện và cách xử lý:** Thay URL bài bún riêu trả 404 bằng nguồn Phở Hà Nội còn hoạt động; thêm tùy chọn `ALLOW_INSECURE_SSL` cho lỗi chứng chỉ của website nguồn; tắt plugin pytest toàn cục bị lỗi `xxhash`. Test còn lại thất bại vì chưa chạy evaluation để sinh `group_project/evaluation/RESULT.md`.

## Điều còn hạn chế

- **Hạn chế:** ChromaDB/Ragas chưa được cài hoàn tất trên máy hiện tại, nên chưa index 1.156 chunks, chưa kiểm chứng PageIndex và chưa có số liệu A/B thật.
- **Nếu có thêm thời gian:** Cài đủ dependency, chạy indexing và 15 golden queries; sau đó hiệu chỉnh `SCORE_THRESHOLD`, hoàn thiện `RESULT.md` và bổ sung nguồn chính thức về giờ mở cửa/giá vé.

## Xác nhận đóng góp

Tôi xác nhận nội dung trên phản ánh đúng phần việc của mình và có thể giải thích hoặc chạy lại trong buổi demo.

- Ngày: 25/09/2026
- Tên thành viên: Nguyễn Thị Bảo Trang
